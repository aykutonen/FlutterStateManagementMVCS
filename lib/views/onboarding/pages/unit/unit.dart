import 'package:flutter/cupertino.dart';

class OnboardingUnitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Onboarding Unit Page'),
      ),
    );
  }
}
